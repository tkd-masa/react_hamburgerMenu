import Logo from "components/logo";
import Nav from "components/nav";

const Header = () => {
  return (
    <header>
      <div>
        <Logo />
        <Nav />
      </div>
    </header>
  );
};

export default Header;
